# User Flow для валидации UX/UI — Dataminer

## Обзор продукта

**Dataminer** — Chrome расширение для визуального извлечения данных с веб-страниц. Пользователь кликает на элементы на странице, и расширение автоматически извлекает все похожие элементы, создавая таблицу данных для экспорта.

**Архитектура:** Side Panel (панель открывается рядом со страницей, не поверх неё)

**⚠️ Важное примечание:** В документации (README, CHANGELOG) упоминаются функции Auto-select и Auto-stop, но в текущей версии кода они **не реализованы**. Пользователь должен вручную запускать и останавливать режим выбора.

---

## Основные пользовательские сценарии

### Сценарий 1: Первое использование (новый пользователь)
### Сценарий 2: Повторное использование (сохранённые поля)
### Сценарий 3: Редактирование существующих полей
### Сценарий 4: Экспорт данных

---

## Детальный User Flow

### 🎯 **Сценарий 1: Первое использование (0 полей)**

#### Шаг 1.1: Открытие расширения
**Действие пользователя:** Клик на иконку расширения в панели инструментов Chrome

**Результат:**
- Открывается Side Panel справа от страницы
- Панель занимает фиксированную ширину (обычно ~400px)
- Страница остаётся видимой и интерактивной

**UI Состояние:**
```
┌─────────────────────────────────┐
│ [Logo] Dataminer    [● Ready]  │ ← Header
├─────────────────────────────────┤
│ [▶ Select Elements]             │ ← Кнопка активна
│ [Clear All] (disabled)          │
├─────────────────────────────────┤
│ 0 columns    0 rows             │ ← Статистика
├─────────────────────────────────┤
│         📊                      │
│  Click "Select Elements"        │ ← Empty state
│  to start                       │
│                                 │
│  Then click on elements on      │
│  the page to add them as        │
│  columns                        │
├─────────────────────────────────┤
│ [⬇ Export CSV] (disabled)      │
│ [⬇ Export JSON] (disabled)     │
└─────────────────────────────────┘
```

**Визуальные элементы:**
- Header: логотип, название, индикатор статуса (зелёная точка + "Ready")
- Кнопка "Select Elements" — основной CTA, выделена (primary стиль)
- Empty state — иконка 📊, текст с инструкцией
- Кнопки экспорта — disabled (серые, неактивные)

---

#### Шаг 1.2: Запуск режима выбора
**Действие пользователя:** Клик на кнопку "Select Elements"

**Результат:**
- Режим выбора активируется
- Кнопка меняется на "⏸ Stop Selection"
- Статус меняется на "Selecting..." (синяя точка)
- На странице активируется режим hover-подсветки элементов

**Примечание:** В текущей версии кода автоматический запуск (auto-select) при 0 полях **не реализован**. Пользователь должен вручную нажать кнопку "Select Elements".

**UI Состояние панели:**
```
┌─────────────────────────────────┐
│ [Logo] Dataminer  [● Selecting] │ ← Статус изменился
├─────────────────────────────────┤
│ [⏸ Stop Selection] (active)     │ ← Кнопка изменилась
│ [Clear All] (disabled)          │
├─────────────────────────────────┤
│ 0 columns    0 rows             │
├─────────────────────────────────┤
│         👆                      │
│  Click elements on the page     │ ← Empty state обновлён
│                                 │
│  Each click adds a column       │
│  to your export                 │
├─────────────────────────────────┤
│ [⬇ Export CSV] (disabled)       │
│ [⬇ Export JSON] (disabled)      │
└─────────────────────────────────┘
```

**UI Состояние страницы:**
- Курсор меняется на `crosshair` при наведении на элементы
- При hover на элемент:
  - Синяя обводка (`outline: 2px solid #6366f1`)
  - Полупрозрачный синий фон (`rgba(99, 102, 241, 0.1)`)
  - Появляется tooltip с превью данных:
    ```
    ┌─────────────────────────────┐
    │ .product-title              │ ← CSS селектор
    │ Type: text                  │ ← Тип данных
    │ Preview: "iPhone 15 Pro"    │ ← Превью значения
    └─────────────────────────────┘
    ```
  - Tooltip следует за курсором, позиционируется рядом с элементом

**Визуальные индикаторы:**
- Синяя точка в статусе (selecting state)
- Кнопка "Stop Selection" в активном состоянии (возможно, другой цвет/стиль)
- Body панели получает класс `selecting-mode` (для возможных стилей)

---

#### Шаг 1.3: Выбор первого элемента
**Действие пользователя:** Клик на элемент на странице (например, название товара)

**Результат:**
- Элемент получает зелёную обводку (выбран)
- Поле добавляется в панель
- Автоматически запускается поиск всех похожих элементов на странице
- Превью таблицы начинает формироваться

**UI Состояние панели:**
```
┌─────────────────────────────────┐
│ [Logo] Dataminer  [● Selecting] │
├─────────────────────────────────┤
│ [⏸ Stop Selection] (active)     │
│ [Clear All] (enabled)           │ ← Теперь активна
├─────────────────────────────────┤
│ 1 column    15 rows             │ ← Статистика обновилась
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ [Product Title        ×]    │ │ ← Заголовок таблицы
│ ├─────────────────────────────┤ │
│ │ iPhone 15 Pro              │ │
│ │ Samsung Galaxy S24         │ │
│ │ ...                         │ │ ← Показывается до 20 строк
│ └─────────────────────────────┘ │
│ ...and 0 more rows              │ ← Если больше 20
├─────────────────────────────────┤
│ [⬇ Export CSV] (enabled)       │ ← Теперь активны
│ [⬇ Export JSON] (enabled)       │
└─────────────────────────────────┘
```

**UI Состояние страницы:**
- Выбранный элемент: зелёная обводка (`outline: 2px solid #22c55e`)
- Все найденные похожие элементы: оранжевая пунктирная обводка (`outline: 2px dashed #f59e0b`)
- Tooltip продолжает показываться при hover на другие элементы

**Визуальные индикаторы:**
- Заголовок таблицы: редактируемое поле ввода (можно переименовать колонку)
- Кнопка "×" в заголовке — удалить колонку
- Таблица появляется с анимацией (fade-in или slide-in)

---

#### Шаг 1.4: Выбор второго и последующих элементов
**Действие пользователя:** Продолжает кликать на другие элементы (цена, изображение, ссылка и т.д.)

**Результат:**
- Каждый клик добавляет новую колонку
- Превью таблицы обновляется в реальном времени
- Статистика обновляется (количество колонок и строк)

**UI Состояние панели:**
```
┌─────────────────────────────────┐
│ [Logo] Dataminer  [● Selecting] │
├─────────────────────────────────┤
│ [⏸ Stop Selection] (active)     │
│ [Clear All] (enabled)           │
├─────────────────────────────────┤
│ 3 columns    15 rows            │ ← Обновлено
├─────────────────────────────────┤
│ ┌──────────────────────────────┐│
│ │[Title ×][Price ×][Image ×]  ││ ← 3 колонки
│ ├──────────────────────────────┤│
│ │iPhone 15 Pro│$999│[img]     ││
│ │Samsung S24  │$899│[img]     ││
│ │...                           ││
│ └──────────────────────────────┘│
├─────────────────────────────────┤
│ [⬇ Export CSV] (enabled)        │
│ [⬇ Export JSON] (enabled)        │
└─────────────────────────────────┘
```

**Особенности:**
- Каждая колонка имеет редактируемое название
- Каждая колонка имеет кнопку удаления "×"
- Превью обновляется мгновенно после каждого клика
- Если данных много, показывается "Calculating preview..." до готовности

---

#### Шаг 1.5: Остановка режима выбора
**Действие пользователя:** Один из способов:
- Клик на кнопку "Stop Selection" в панели
- Нажатие клавиши `Esc` на клавиатуре

**Результат:**
- Режим выбора останавливается
- Кнопка возвращается к "▶ Select Elements"
- Статус меняется на "Ready"
- Hover-подсветка на странице отключается
- Tooltip исчезает

**UI Состояние панели:**
```
┌─────────────────────────────────┐
│ [Logo] Dataminer    [● Ready]  │ ← Статус вернулся
├─────────────────────────────────┤
│ [▶ Select Elements]             │ ← Кнопка вернулась
│ [Clear All] (enabled)           │
├─────────────────────────────────┤
│ 3 columns    15 rows            │
├─────────────────────────────────┤
│ [Таблица с данными]             │ ← Превью остаётся
├─────────────────────────────────┤
│ [⬇ Export CSV] (enabled)        │
│ [⬇ Export JSON] (enabled)        │
└─────────────────────────────────┘
```

**UI Состояние страницы:**
- Все визуальные индикаторы (обводки, tooltip) исчезают
- Курсор возвращается к обычному виду
- Страница полностью интерактивна

**Примечание:** В текущей версии кода автоматическая остановка через 2 секунды бездействия (auto-stop) **не реализована**. Пользователь должен вручную остановить выбор.

---

#### Шаг 1.6: Экспорт данных
**Действие пользователя:** Клик на "Export CSV" или "Export JSON"

**Результат:**
- Файл скачивается через `chrome.downloads` API
- Появляется toast-уведомление об успехе

**UI Состояние:**
- Toast появляется внизу панели:
  ```
  ┌─────────────────────────────┐
  │ ✓ Exported 15 rows to CSV  │ ← Зелёный toast
  └─────────────────────────────┘
  ```
- Toast исчезает через 3 секунды с анимацией slide-up

**Формат файла:**
- CSV: `dataminer-export-YYYY-MM-DD (site-name).csv`
- JSON: `dataminer-export-YYYY-MM-DD (site-name).json`

---

### 🎯 **Сценарий 2: Повторное использование (сохранённые поля)**

#### Шаг 2.1: Открытие расширения на том же сайте
**Действие пользователя:** Клик на иконку расширения на сайте, где уже были созданы поля

**Результат:**
- Панель открывается
- Автоматически загружаются сохранённые поля для этого сайта (origin)
- Превью таблицы генерируется автоматически
- Режим выбора НЕ запускается автоматически (т.к. есть поля)

**UI Состояние:**
```
┌─────────────────────────────────┐
│ [Logo] Dataminer    [● Ready]  │
├─────────────────────────────────┤
│ [▶ Select Elements]             │
│ [Clear All] (enabled)           │
├─────────────────────────────────┤
│ 3 columns    15 rows            │ ← Сохранённые данные
├─────────────────────────────────┤
│ [Таблица с данными]             │ ← Превью готово
├─────────────────────────────────┤
│ [⬇ Export CSV] (enabled)        │
│ [⬇ Export JSON] (enabled)        │
└─────────────────────────────────┘
```

**Особенности:**
- Состояние сохраняется отдельно для каждого сайта (origin)
- Если пользователь перешёл на другую вкладку с другим сайтом, загружается состояние для того сайта
- Если данных много, может показываться "Calculating preview..." до готовности

---

#### Шаг 2.2: Добавление новых полей к существующим
**Действие пользователя:** Клик на "Select Elements"

**Результат:**
- Активируется режим выбора
- Пользователь может добавить новые колонки к существующим
- Превью обновляется с новыми данными

**UI Состояние:**
- Аналогично Шагу 1.2, но таблица уже видна
- Новые колонки добавляются справа в таблице

---

### 🎯 **Сценарий 3: Редактирование существующих полей**

#### Шаг 3.1: Переименование колонки
**Действие пользователя:** Клик на название колонки в заголовке таблицы → ввод нового названия

**Результат:**
- Название колонки обновляется
- Изменение сохраняется с задержкой 500ms (debounce)
- Превью обновляется с новым названием

**UI Состояние:**
```
┌─────────────────────────────────┐
│ [Product Title        ×]        │ ← До редактирования
│                                 │
│ [iPhone 15 Pro        ×]        │ ← Во время редактирования
│  ^редактируемое поле            │
│                                 │
│ [Название товара      ×]        │ ← После редактирования
└─────────────────────────────────┘
```

**Визуальные индикаторы:**
- Поле ввода получает фокус
- Возможно, подсветка или border при редактировании
- Изменения применяются автоматически (не нужно нажимать Enter)

---

#### Шаг 3.2: Удаление колонки
**Действие пользователя:** Клик на кнопку "×" в заголовке колонки

**Результат:**
- Колонка удаляется из таблицы
- Превью обновляется
- Статистика обновляется (количество колонок уменьшается)

**UI Состояние:**
- Колонка исчезает с анимацией (fade-out или slide-out)
- Если колонок не осталось, показывается empty state
- Кнопка "Clear All" становится disabled, если колонок 0

---

#### Шаг 3.3: Очистка всех полей
**Действие пользователя:** Клик на "Clear All"

**Результат:**
- Все поля удаляются
- Превью очищается
- Показывается empty state
- Появляется toast: "All fields cleared"

**UI Состояние:**
- Возвращается к состоянию Шага 1.1
- Toast-уведомление: "All fields cleared" (success, зелёный)

---

### 🎯 **Сценарий 4: Экспорт данных**

#### Шаг 4.1: Экспорт CSV
**Действие пользователя:** Клик на "Export CSV" или `Ctrl+E` / `Cmd+E`

**Результат:**
- CSV файл скачивается
- Toast: "Exported X rows to CSV"

**Формат CSV:**
- Заголовки: названия колонок (как в таблице)
- Данные: все строки из превью
- Кодировка: UTF-8
- Разделитель: запятая

---

#### Шаг 4.2: Экспорт JSON
**Действие пользователя:** Клик на "Export JSON"

**Результат:**
- JSON файл скачивается
- Toast: "Exported X rows to JSON"

**Формат JSON:**
```json
[
  {
    "Product Title": "iPhone 15 Pro",
    "Price": "$999",
    "Image": "https://..."
  },
  ...
]
```

---

## Состояния UI компонентов

### 1. Header (Заголовок панели)

**Элементы:**
- Логотип (28x28px, скруглённые углы)
- Название "Dataminer" (градиентный текст)
- Индикатор статуса:
  - **Ready**: зелёная точка + "Ready"
  - **Selecting**: синяя точка + "Selecting..." или "Click elements on page"
  - **Error**: красная точка + текст ошибки

**Стили:**
- Тёмный фон (градиент)
- Нижняя граница (subtle)

---

### 2. Кнопка "Select Elements" / "Stop Selection"

**Состояния:**
- **Default**: "▶ Select Elements" (primary стиль, синяя)
- **Active/Selecting**: "⏸ Stop Selection" (возможно, другой цвет или стиль)
- **Disabled**: (не используется в текущем flow)

**Стили:**
- Primary button (акцентный цвет)
- Иконка + текст
- Hover эффект
- Active state при selecting

---

### 3. Кнопка "Clear All"

**Состояния:**
- **Enabled**: красная/опасная кнопка (danger стиль)
- **Disabled**: серая, неактивная (когда 0 полей)

**Стили:**
- Danger button (красный акцент)
- Только текст (без иконки)

---

### 4. Статистика (Stats)

**Элементы:**
- Количество колонок: `X columns`
- Количество строк: `Y rows`

**Стили:**
- Два блока рядом
- Большое число (значение)
- Маленький текст (label)
- Мuted цвет для label

---

### 5. Empty State

**Элементы:**
- Иконка (📊 или кастомная)
- Основной текст
- Подсказка (hint)

**Состояния:**
- **Default**: "Click 'Select Elements' to start" + подсказка
- **Selecting**: "👆 Click elements on the page" + "Each click adds a column"

**Стили:**
- Центрированное содержимое
- Крупная иконка
- Muted цвет текста

---

### 6. Таблица превью (Preview Table)

**Элементы:**
- Заголовки (thead):
  - Редактируемое поле ввода (название колонки)
  - Кнопка удаления "×"
- Тело таблицы (tbody):
  - До 20 строк видимо
  - Обрезка длинного текста (100 символов)
  - Tooltip при hover на ячейку (полный текст)

**Стили:**
- Тёмная тема
- Скруглённые углы
- Hover на строке
- Scroll, если данных много

**Индикатор "more rows":**
- Если строк > 20: "...and X more rows" под таблицей

---

### 7. Кнопки экспорта

**Элементы:**
- "⬇ Export CSV"
- "⬇ Export JSON"

**Состояния:**
- **Enabled**: активные (когда есть данные)
- **Disabled**: серые, неактивные (когда 0 строк)

**Стили:**
- Export button стиль
- Иконка ⬇ + текст
- Hover эффект

---

### 8. Toast уведомления

**Типы:**
- **Success**: зелёный (экспорт успешен, очистка успешна)
- **Error**: красный (ошибки)
- **Info**: синий (информационные)

**Анимация:**
- Появление: slide-up
- Исчезновение: slide-up reverse
- Длительность показа: 3 секунды

**Позиция:**
- Внизу панели
- Контейнер для нескольких toast (stack)

---

## Интерактивные элементы на странице

### 1. Hover подсветка элемента

**Визуальные эффекты:**
- Синяя обводка: `outline: 2px solid #6366f1`
- Синий фон: `rgba(99, 102, 241, 0.1)`
- Курсор: `crosshair`
- Transition: `0.15s ease`

**Когда показывается:**
- Только в режиме выбора (`isSelecting = true`)
- При наведении на любой элемент страницы

---

### 2. Tooltip с превью данных

**Содержимое:**
- CSS селектор элемента (моноширинный шрифт, muted цвет)
- Тип данных: "Type: text" / "Type: image" / "Type: link"
- Превью значения (обрезается, если длинное)

**Стили:**
- Тёмный фон (градиент)
- Скруглённые углы
- Тень
- Следует за курсором
- Максимальная ширина: 320px

**Позиционирование:**
- Рядом с элементом (не перекрывает)
- Автоматическое позиционирование (сверху/снизу/слева/справа)

---

### 3. Выбранный элемент

**Визуальные эффекты:**
- Зелёная обводка: `outline: 2px solid #22c55e`
- Зелёный фон: `rgba(34, 197, 94, 0.1)`
- Класс: `onpage-selected-element`

**Когда показывается:**
- Сразу после клика на элемент
- Остаётся до остановки режима выбора

---

### 4. Найденные похожие элементы (Preview highlight)

**Визуальные эффекты:**
- Оранжевая пунктирная обводка: `outline: 2px dashed #f59e0b`
- Оранжевый фон: `rgba(245, 158, 11, 0.1)`
- Класс: `dataminer-preview-highlight`

**Когда показывается:**
- После выбора элемента
- На всех найденных похожих элементах на странице
- Помогает пользователю увидеть, какие данные будут извлечены

---

## Клавиатурные сокращения (Keyboard Shortcuts)

1. **`Esc`** — остановить режим выбора
   - Работает только когда `isSelecting = true`
   - Останавливает выбор и возвращает панель в состояние "Ready"

2. **`Ctrl+E`** / **`Cmd+E`** — быстрый экспорт CSV
   - Работает когда есть данные (`previewRows.length > 0`)
   - Предотвращает стандартное поведение (preventDefault)
   - Триггерит экспорт CSV

---

## Сохранение состояния (State Persistence)

**Механизм:**
- Состояние сохраняется отдельно для каждого `origin` (домена)
- Ключ: `dataminer_state_{origin}`
- Сохраняется в `chrome.storage.local`

**Что сохраняется:**
- Массив полей (`fields`): селекторы, названия, типы
- Временная метка обновления (`updatedAt`)

**Когда сохраняется:**
- При добавлении поля
- При удалении поля
- При переименовании поля (debounce 500ms)
- При очистке всех полей

**Когда загружается:**
- При открытии панели
- При переключении вкладок
- При навигации на странице (если origin не изменился)

---

## Обработка ошибок и edge cases

### 1. Ошибка инъекции content script
**Симптом:** Не удаётся начать режим выбора

**UI:**
- Toast: "Cannot start selection. Refresh the page." (error, красный)
- Статус: "Error" (красная точка)

**Решение для пользователя:** Обновить страницу

---

### 2. Нет активной вкладки
**Симптом:** Панель открыта, но нет активной вкладки

**UI:**
- Статус: "Error" + "No active tab"

---

### 3. Страница не загружена
**Симптом:** Content script не может выполниться

**UI:**
- Toast: "Cannot start selection. Refresh the page." (error)
- Автоматическая инъекция скрипта при следующей попытке

---

### 4. Нет данных для превью
**Симптом:** Поля есть, но элементы не найдены на странице

**UI:**
- Таблица показывает: "Calculating preview..." или пустые ячейки
- Статистика: `X columns    0 rows`
- Кнопки экспорта: disabled

---

### 5. Очень длинный текст в ячейке
**Симптом:** Текст обрезается до 100 символов

**UI:**
- В ячейке: обрезанный текст + "..."
- При hover: tooltip с полным текстом (title attribute)

---

## Анимации и переходы

### 1. Появление панели
- Side Panel открывается с анимацией slide-in справа

### 2. Появление таблицы
- Fade-in или slide-in при первом появлении
- Плавное обновление при добавлении колонок

### 3. Исчезновение колонки
- Fade-out или slide-out при удалении

### 4. Toast уведомления
- Появление: slide-up (снизу вверх)
- Исчезновение: slide-up reverse (вверх вниз)

### 5. Hover эффекты
- Плавные переходы (transition) на всех интерактивных элементах
- Обводка и фон элементов: `transition: all 0.15s ease`

---

## Цветовая схема (для справки)

**Фоны:**
- Primary: `#0c0c0f` (очень тёмный)
- Secondary: `#13131a`
- Tertiary: `#1a1a24`
- Hover: `#22222e`

**Акценты:**
- Primary: `#6366f1` (синий/индиго)
- Success: `#22c55e` (зелёный)
- Danger: `#ef4444` (красный)
- Warning: `#f59e0b` (оранжевый)

**Текст:**
- Primary: `#f4f4f5` (почти белый)
- Secondary: `#a1a1aa` (серый)
- Muted: `#71717a` (тёмно-серый)

---

## Чек-лист для валидации UX/UI

### Функциональность
- [ ] Панель открывается корректно при клике на иконку
- [ ] Кнопка "Select Elements" запускает режим выбора
- [ ] Кнопка "Stop Selection" останавливает режим выбора
- [ ] Клавиша `Esc` останавливает режим выбора
- [ ] Hover подсветка работает на странице
- [ ] **⚠️ Auto-select при 0 полях НЕ реализован** (требуется ручной клик)
- [ ] **⚠️ Auto-stop через 2 секунды НЕ реализован** (требуется ручная остановка)
- [ ] Tooltip показывается при hover
- [ ] Клик на элемент добавляет поле
- [ ] Превью обновляется в реальном времени
- [ ] Переименование колонки работает
- [ ] Удаление колонки работает
- [ ] Очистка всех полей работает
- [ ] Экспорт CSV работает
- [ ] Экспорт JSON работает
- [ ] Сохранение состояния работает (per origin)
- [ ] Клавиатурные сокращения работают

### Визуальный дизайн
- [ ] Цветовая схема соответствует тёмной теме
- [ ] Все интерактивные элементы имеют hover эффекты
- [ ] Анимации плавные и не раздражающие
- [ ] Типографика читабельна
- [ ] Иконки понятны и соответствуют действиям
- [ ] Empty state информативен
- [ ] Таблица превью читабельна
- [ ] Toast уведомления заметны, но не навязчивы

### UX
- [ ] Пользователь понимает, что делать на каждом шаге
- [ ] Минимум кликов для типичного сценария (5 кликов для 3 полей → CSV)
- [ ] Обратная связь на каждое действие (визуальная или toast)
- [ ] Ошибки понятны и предлагают решение
- [ ] Минимум шагов для типичного сценария
- [ ] **⚠️ Примечание:** Auto-select и auto-stop описаны в документации, но не реализованы в коде
- [ ] Состояния UI понятны (Ready, Selecting, Error)
- [ ] Клавиатурные сокращения интуитивны

### Доступность
- [ ] Контрастность текста достаточна
- [ ] Интерактивные элементы имеют достаточный размер для клика
- [ ] Фокус виден на элементах формы
- [ ] Tooltip доступен (не только hover, но и через title)

---

## Дополнительные заметки для дизайнера

1. **Side Panel ограничения:**
   - Фиксированная ширина (обычно ~400px, зависит от браузера)
   - Нельзя изменять размер пользователем
   - Всегда справа от страницы

2. **Content Script ограничения:**
   - Работает только на веб-страницах (не на chrome:// страницах)
   - Может быть заблокирован на некоторых сайтах (CSP)
   - Инъекция происходит автоматически при необходимости

3. **Производительность:**
   - Превью ограничено 20 строками для отображения (для производительности)
   - Экспорт включает все строки (не только видимые)
   - Обрезка текста в ячейках (100 символов) для читабельности

4. **Совместимость:**
   - Требуется Chrome 114+ (для Side Panel API)
   - Работает в Edge (Chromium)
   - Не работает в Firefox (нет Side Panel API)

---

## Контакты и вопросы

Если у дизайнера возникнут вопросы по user flow или нужно уточнить детали реализации, можно обратиться к:
- README.md — общая документация
- CHANGELOG.md — история изменений
- Код: `extension/sidepanel.js` и `extension/content.js`

