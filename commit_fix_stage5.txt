fix(ux-ui): Исправление - полная таблица не появлялась после Stop Selection

- Добавлен вызов this.render() в методе stopSelection()
- Добавлен вызов this.render() в обработчике сообщения 'selectionStopped'
- Теперь после остановки режима Selecting корректно отображается полная таблица вместо компактного preview

Исправляет проблему из Этапа 5

