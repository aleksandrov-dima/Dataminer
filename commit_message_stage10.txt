feat(ux-ui): Этап 10 - улучшение сообщений об ошибках

- Все сообщения об ошибках используют одно короткое предложение
- Все сообщения об ошибках содержат действие пользователя ("Refresh the page")
- Улучшены сообщения об ошибках:
  - "Error clearing fields" → "Cannot clear fields. Refresh the page."
  - "Export failed" → "Export failed. Refresh the page and try again."
- После ошибки в startSelection() UI возвращается в состояние Idle:
  - isSelecting = false
  - Обновляется кнопка (возвращается к "Select Elements")
  - Обновляется статус (возвращается к "Ready to select")
- Технические детали (console.log) используются только для отладки
- Пользователю показываются понятные сообщения без технических деталей
- Добавлены тесты для проверки формата и содержания сообщений об ошибках

Все тесты прошли успешно (107/107)

